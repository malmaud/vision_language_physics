<Window x:Class="kinect_recorder_gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kinect_recorder_gui"
        mc:Ignorable="d"
        Title="Recorder" Height="350" Width="525" Closing="Window_Closing">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
            <Label Grid.Row="0" Grid.Column="0">Directory:</Label>
            <TextBox Grid.Row="0" Grid.Column="1" Name="directoryText"/>
            <Label Grid.Row="1" Grid.Column="0">Video ID:</Label>
            <TextBox Grid.Row="1" Grid.Column="1" Name="videoIDText"/>
        <Button Grid.Row="2" Grid.Column="2" Name="recordButton" Height="50" VerticalAlignment="Top" Click="recordButton_Click">
            Record
        </Button>
    </Grid>
</Window>
