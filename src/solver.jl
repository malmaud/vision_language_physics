module Solver

import Base: *

immutable Lattice
end

immutable State
end

function Base.cross(l1::Lattice, l2::Lattice)
    s1 = l1
    for i=1:5
        y=i+3
    end
end

myfunc(x::Float64)=1
myfunc(x::Int)=2



end
